---
- hosts: all
  connection: local
  become: yes
  vars_files:
  - vars/secrets.yml

  handlers:
  - include: handlers/main.yml

  post_tasks:
  - {include: tasks/cisco-anyconnect-client.yml, tags: vpn}
  - {include: tasks/play.yml, play_version: '2.2.6', tags: play}
