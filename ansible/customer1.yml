---
- hosts: all
  connection: local
  become: yes
  vars_files:
  - vars/secrets.yml

  handlers:
  - include: handlers/main.yml

  post_tasks:
  - {include: tasks/cisco-vpn-client.yml, tags: [vpn, vpnc]}
  - {include: tasks/hipchat.yml, tags: hipchat}
  - {include: tasks/erlang.yml, tags: erlang, distribution_release: xenial}
